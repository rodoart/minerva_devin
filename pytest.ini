[pytest]
testpaths = tests
markers =
    spark: tests that require a local SparkSession (skipped automatically if pyspark is not installed)
    graphframes: tests that require GraphFrames (skipped automatically if the package/jar is unavailable)
